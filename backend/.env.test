# Use a separate test database so tests don't wipe your dev data
PORT=5001
NODE_ENV=test

DB_HOST=localhost
DB_PORT=3307
DB_NAME=tms_db_test
DB_USER=root
DB_PASSWORD=1234

JWT_SECRET=test_secret
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=test_refresh_secret
JWT_REFRESH_EXPIRES_IN=1d

EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=587
EMAIL_USER=test
EMAIL_PASS=test
EMAIL_FROM=noreply@tms.com

FRONTEND_URL=http://localhost:3000
